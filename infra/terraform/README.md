# Terraform (skeleton)

This is a minimal starting point. Extend with ECS/Fargate services, ALB, IAM roles, and Secrets Manager.

## Quickstart
```bash
terraform init
terraform apply -var 'aws_region=<REGION>' -var 'project_name=aws-fastapi-starter'
```
